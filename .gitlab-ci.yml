image: ruby:3.4.4

build:
    before_script:
    - ruby -v
    - bundle install --path vendor/bundle
    script:
    - rake

pages:
  stage: deploy
  script:
    - mkdir .public
    - cp -r * .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - master
